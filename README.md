# Basic authentication using session.

client_user_id is stored in session which is used as an identifier for User.  
On log_out session is wipped.

# Setup

- bundle install
- rails db:create db:migrate
